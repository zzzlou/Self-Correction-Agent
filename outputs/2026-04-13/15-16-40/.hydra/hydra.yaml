hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath:
  - pkg://verl.trainer.config
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - algorithm.adv_estimator=grpo
    - data.train_batch_size=16
    - data.val_batch_size=512
    - data.max_prompt_length=4096
    - data.max_response_length=2048
    - actor_rollout_ref.model.path=deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B
    - actor_rollout_ref.hybrid_engine=True
    - actor_rollout_ref.actor.optim.lr=1e-6
    - actor_rollout_ref.actor.strategy=fsdp2
    - actor_rollout_ref.actor.loss_agg_mode=seq-mean-token-sum-norm
    - actor_rollout_ref.model.use_remove_padding=True
    - actor_rollout_ref.actor.ppo_mini_batch_size=8
    - actor_rollout_ref.actor.use_dynamic_bsz=True
    - actor_rollout_ref.actor.ppo_max_token_len_per_gpu=30000
    - actor_rollout_ref.actor.use_kl_loss=False
    - actor_rollout_ref.actor.clip_ratio_high=0.28
    - actor_rollout_ref.actor.kl_loss_coef=0.001
    - actor_rollout_ref.actor.kl_loss_type=low_var_kl
    - actor_rollout_ref.actor.ulysses_sequence_parallel_size=1
    - actor_rollout_ref.model.enable_gradient_checkpointing=True
    - actor_rollout_ref.actor.fsdp_config.param_offload=True
    - actor_rollout_ref.actor.fsdp_config.optimizer_offload=False
    - actor_rollout_ref.rollout.calculate_log_probs=True
    - actor_rollout_ref.rollout.tensor_model_parallel_size=1
    - actor_rollout_ref.rollout.name=vllm
    - actor_rollout_ref.rollout.mode=async
    - actor_rollout_ref.rollout.enforce_eager=False
    - actor_rollout_ref.rollout.temperature=0.6
    - actor_rollout_ref.rollout.gpu_memory_utilization=0.6
    - actor_rollout_ref.rollout.n=8
    - actor_rollout_ref.rollout.val_kwargs.n=1
    - actor_rollout_ref.rollout.val_kwargs.temperature=0.6
    - actor_rollout_ref.rollout.val_kwargs.top_p=0.9
    - actor_rollout_ref.ref.fsdp_config.param_offload=True
    - actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1
    - actor_rollout_ref.actor.entropy_coeff=0
    - algorithm.kl_ctrl.kl_coef=0.001
    - rllm.mask_truncated_samples=False
    - trainer.critic_warmup=0
    - trainer.logger=[console,wandb]
    - trainer.project_name=rllm-agent
    - trainer.experiment_name=two-attempt-math
    - trainer.val_before_train=True
    - trainer.n_gpus_per_node=2
    - trainer.nnodes=1
    - trainer.save_freq=500
    - trainer.test_freq=30
    - trainer.default_hdfs_dir=null
    - trainer.total_epochs=100
    - rllm.stepwise_advantage.enable=True
    - rllm.stepwise_advantage.mode=per_step
    - rllm.agent.max_steps=2
  job:
    name: train
    chdir: null
    override_dirname: actor_rollout_ref.actor.clip_ratio_high=0.28,actor_rollout_ref.actor.entropy_coeff=0,actor_rollout_ref.actor.fsdp_config.optimizer_offload=False,actor_rollout_ref.actor.fsdp_config.param_offload=True,actor_rollout_ref.actor.kl_loss_coef=0.001,actor_rollout_ref.actor.kl_loss_type=low_var_kl,actor_rollout_ref.actor.loss_agg_mode=seq-mean-token-sum-norm,actor_rollout_ref.actor.optim.lr=1e-6,actor_rollout_ref.actor.ppo_max_token_len_per_gpu=30000,actor_rollout_ref.actor.ppo_mini_batch_size=8,actor_rollout_ref.actor.strategy=fsdp2,actor_rollout_ref.actor.ulysses_sequence_parallel_size=1,actor_rollout_ref.actor.use_dynamic_bsz=True,actor_rollout_ref.actor.use_kl_loss=False,actor_rollout_ref.hybrid_engine=True,actor_rollout_ref.model.enable_gradient_checkpointing=True,actor_rollout_ref.model.path=deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B,actor_rollout_ref.model.use_remove_padding=True,actor_rollout_ref.ref.fsdp_config.param_offload=True,actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.calculate_log_probs=True,actor_rollout_ref.rollout.enforce_eager=False,actor_rollout_ref.rollout.gpu_memory_utilization=0.6,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.mode=async,actor_rollout_ref.rollout.n=8,actor_rollout_ref.rollout.name=vllm,actor_rollout_ref.rollout.temperature=0.6,actor_rollout_ref.rollout.tensor_model_parallel_size=1,actor_rollout_ref.rollout.val_kwargs.n=1,actor_rollout_ref.rollout.val_kwargs.temperature=0.6,actor_rollout_ref.rollout.val_kwargs.top_p=0.9,algorithm.adv_estimator=grpo,algorithm.kl_ctrl.kl_coef=0.001,data.max_prompt_length=4096,data.max_response_length=2048,data.train_batch_size=16,data.val_batch_size=512,rllm.agent.max_steps=2,rllm.mask_truncated_samples=False,rllm.stepwise_advantage.enable=True,rllm.stepwise_advantage.mode=per_step,trainer.critic_warmup=0,trainer.default_hdfs_dir=null,trainer.experiment_name=two-attempt-math,trainer.logger=[console,wandb],trainer.n_gpus_per_node=2,trainer.nnodes=1,trainer.project_name=rllm-agent,trainer.save_freq=500,trainer.test_freq=30,trainer.total_epochs=100,trainer.val_before_train=True
    id: ???
    num: ???
    config_name: agent_ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /home/zhanzhi/Self-Correction-Agent
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: rllm.trainer.config
      schema: pkg
      provider: main
    - path: verl.trainer.config
      schema: pkg
      provider: hydra.searchpath in main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /home/zhanzhi/Self-Correction-Agent/outputs/2026-04-13/15-16-40
    choices:
      reward_model: dp_reward_model
      critic: dp_critic
      rollout@actor_rollout_ref.rollout: rollout
      ref@actor_rollout_ref.ref: dp_ref
      data: legacy_data
      npu_profile@trainer.npu_profile: npu_profile
      actor@actor_rollout_ref.actor: dp_actor
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
